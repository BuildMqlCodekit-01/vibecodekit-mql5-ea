# trend / netting

Trend-following EA: 50/200 MA cross with CPipNormalizer SL/TP.

Render via:

```bash
python -m vibecodekit_mql5.build trend \
    --name MyEA --symbol EURUSD --tf H1 --stack netting
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
