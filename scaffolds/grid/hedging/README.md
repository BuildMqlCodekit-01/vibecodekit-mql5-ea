# grid / hedging

Grid trader with pip-normalized step + max-step cap (AP-4 aware).

Render via:

```bash
python -m vibecodekit_mql5.build grid \
    --name MyEA --symbol EURUSD --tf H1 --stack hedging
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
