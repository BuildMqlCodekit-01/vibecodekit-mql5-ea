# arbitrage-stat / python-bridge

Statistical arbitrage with cointegration signal from Python bridge.

Render via:

```bash
python -m vibecodekit_mql5.build arbitrage-stat \
    --name MyEA --symbol EURUSD --tf H1 --stack python-bridge
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
