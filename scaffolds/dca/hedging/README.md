# dca / hedging

Dollar-cost averaging EA with timed entries + drawdown freeze.

Render via:

```bash
python -m vibecodekit_mql5.build dca \
    --name MyEA --symbol EURUSD --tf H1 --stack hedging
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
