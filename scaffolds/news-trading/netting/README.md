# news-trading / netting

News-driven straddle: bracket pending orders ahead of releases.

Render via:

```bash
python -m vibecodekit_mql5.build news-trading \
    --name MyEA --symbol EURUSD --tf H1 --stack netting
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
