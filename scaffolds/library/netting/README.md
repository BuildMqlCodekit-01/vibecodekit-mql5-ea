# library / netting

Reusable library scaffold (Include-only). EA shell delegates to includes.

Render via:

```bash
python -m vibecodekit_mql5.build library \
    --name MyEA --symbol EURUSD --tf H1 --stack netting
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
