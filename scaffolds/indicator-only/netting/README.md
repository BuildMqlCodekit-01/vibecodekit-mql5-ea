# indicator-only / netting

Indicator-development scaffold. EA emits signals; no trading.

Render via:

```bash
python -m vibecodekit_mql5.build indicator-only \
    --name MyEA --symbol EURUSD --tf H1 --stack netting
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
