# breakout / netting

Breakout EA: Donchian channel with ATR stop.

Render via:

```bash
python -m vibecodekit_mql5.build breakout \
    --name MyEA --symbol EURUSD --tf H1 --stack netting
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
