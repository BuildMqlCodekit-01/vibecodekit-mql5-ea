# scalping / hedging

M1 scalper with tight spread guard + 5-pip SL.

Render via:

```bash
python -m vibecodekit_mql5.build scalping \
    --name MyEA --symbol EURUSD --tf H1 --stack hedging
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
