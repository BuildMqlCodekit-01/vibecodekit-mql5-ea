# mean-reversion / hedging

Mean-reversion: Bollinger Band fade with pip-normalized SL.

Render via:

```bash
python -m vibecodekit_mql5.build mean-reversion \
    --name MyEA --symbol EURUSD --tf H1 --stack hedging
```

See [Phase D spec](../../../docs/phase-D-spec.md) for context.
